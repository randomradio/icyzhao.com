# Scripts

Planned scripts:

- `import-notion`: export Notion pages marked `Ready` into Markdown.
- `discover-subdomains`: read Cloudflare DNS records and write generated navigation data.
- `validate-content`: check required front matter, duplicate slugs, and URL collisions.

Keep scripts idempotent. Generated files should be safe to rebuild.

